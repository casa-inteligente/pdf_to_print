import tkinter as tk
from print_memorando import Template  # Importe sua classe Template

class InterfaceGrafica:
    def __init__(self, root):
        self.root = root
        self.root.title("Escolha uma função")

        self.imprimir_button = tk.Button(root, text="Imprimir", command=self.imprimir)
        self.imprimir_button.pack()

        self.coletar_button = tk.Button(root, text="Coletar informações", command=self.coletar)
        self.coletar_button.pack()

    def imprimir(self):
        # Chame a função de impressão aqui
        template = Template()
        template.Imprimi_nova()

    def coletar(self):
        # Chame a função de coleta de informações aqui
        pass

if __name__ == "__main__":
    root = tk.Tk()
    app = InterfaceGrafica(root)
    root.mainloop()


#####################################################################################

from kivy.app import App
from kivy.uix.boxlayout import BoxLayout
from kivy.uix.button import Button
from print_memorando import Template  # Importe sua classe Template

class MyApp(App):
    def build(self):
        layout = BoxLayout(orientation='vertical')

        imprimir_button = Button(text='Imprimir', on_press=self.imprimir)
        coletar_button = Button(text='Coletar informações', on_press=self.coletar)

        layout.add_widget(imprimir_button)
        layout.add_widget(coletar_button)

        return layout

    def imprimir(self, instance):
        # Chame a função de impressão aqui
        template = Template()
        template.Imprimi_nova()

    def coletar(self, instance):
        # Chame a função de coleta de informações aqui
        pass

if __name__ == '__main__':
    MyApp().run()
